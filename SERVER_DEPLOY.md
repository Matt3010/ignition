# Server deployment files

The complete files to copy to the target server are stored in [`server-deploy/`](server-deploy/).

Copy the whole folder, create `.env` from `.env.example`, then run `docker compose up -d`.
